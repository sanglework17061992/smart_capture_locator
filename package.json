{
  "name": "smart-locator-inspector",
  "version": "1.0.0",
  "description": "Real-time DOM element inspector with automation-friendly locators using Chrome DevTools Protocol",
  "main": "smart-locator.js",
  "scripts": {
    "start": "node smart-locator.js --modular",
    "build": "cd src && node build.js",
    "build:watch": "cd src && node build.js && echo 'Watching for changes...' && chokidar \"modules/**/*.js\" \"main.js\" -c \"node build.js\"",
    "dev": "npm run build && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "automation",
    "testing",
    "locators",
    "xpath",
    "css-selector",
    "chrome-devtools",
    "inspector",
    "qa-tools"
  ],
  "author": "Smart Locator Inspector",
  "license": "MIT",
  "dependencies": {
    "chrome-remote-interface": "^0.33.3",
    "puppeteer": "^24.23.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "bin": {
    "sli": "./smart-locator.js"
  }
}
